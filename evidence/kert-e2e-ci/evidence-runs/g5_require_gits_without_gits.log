................EsE.E..E.EEE.EE.EEE.EEEE..EEE.E                          [100%]
==================================== ERRORS ====================================
______ ERROR at setup of TestCrossServiceHealth.test_gits_backend_health _______

item = <Function test_gits_backend_health>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_gits_backend_health>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
__ ERROR at setup of TestCrossServiceHealth.test_all_services_simultaneously ___

item = <Function test_all_services_simultaneously>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_all_services_simultaneously>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
_ ERROR at setup of TestGitsToKertIntegration.test_gits_calls_kert_skill_execute _

item = <Function test_gits_calls_kert_skill_execute>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_gits_calls_kert_skill_execute>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
___ ERROR at setup of TestGitsToKertIntegration.test_gits_kert_connectivity ____

item = <Function test_gits_kert_connectivity>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_gits_kert_connectivity>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
_________ ERROR at setup of TestContinuousOperation.test_start_journey _________

item = <Function test_start_journey>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_start_journey>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
________ ERROR at setup of TestContinuousOperation.test_operating_view _________

item = <Function test_operating_view>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_operating_view>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
______ ERROR at setup of TestContinuousOperation.test_gate_state_via_gits ______

item = <Function test_gate_state_via_gits>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_gate_state_via_gits>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
_____ ERROR at setup of TestContinuousOperation.test_full_engagement_loop ______

item = <Function test_full_engagement_loop>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_full_engagement_loop>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
_________ ERROR at setup of TestPrevisitReport.test_memory_extraction __________

item = <Function test_memory_extraction>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_memory_extraction>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
_________ ERROR at setup of TestPrevisitReport.test_full_previsit_flow _________

item = <Function test_full_previsit_flow>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_full_previsit_flow>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
____ ERROR at setup of TestServiceProposal.test_generate_proposal_via_gits _____

item = <Function test_generate_proposal_via_gits>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_generate_proposal_via_gits>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
_______ ERROR at setup of TestServiceProposal.test_proposal_fact_labels ________

item = <Function test_proposal_fact_labels>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_proposal_fact_labels>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
____ ERROR at setup of TestServiceProposal.test_proposal_different_industry ____

item = <Function test_proposal_different_industry>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_proposal_different_industry>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
____ ERROR at setup of TestKnowledgeGraph.test_supply_chain_graph_via_gits _____

item = <Function test_supply_chain_graph_via_gits>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_supply_chain_graph_via_gits>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
______ ERROR at setup of TestKnowledgeGraph.test_supply_chain_node_types _______

item = <Function test_supply_chain_node_types>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_supply_chain_node_types>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
_________ ERROR at setup of TestKnowledgeGraph.test_supply_chain_edges _________

item = <Function test_supply_chain_edges>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_supply_chain_edges>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
______ ERROR at setup of TestCustomerInsight.test_customer_operating_view ______

item = <Function test_customer_operating_view>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_customer_operating_view>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
_________ ERROR at setup of TestCustomerInsight.test_kyc_gap_analysis __________

item = <Function test_kyc_gap_analysis>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_kyc_gap_analysis>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
_____ ERROR at setup of TestCustomerInsight.test_insight_gate_correlation ______

item = <Function test_insight_gate_correlation>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_insight_gate_correlation>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError
_________ ERROR at setup of TestCustomerInsight.test_full_insight_flow _________

item = <Function test_full_insight_flow>

    def pytest_runtest_setup(item: Any) -> None:
        """require 优先于 skip：夹具解析前先判定本用例所需服务是否被 require 且缺失。"""
        if item.nodeid not in _OWN_NODEIDS:
            return
>       _guard_required_for(item)

tests/e2e/conftest.py:343: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

item = <Function test_full_insight_flow>

    def _guard_required_for(item: Any) -> None:
        """在夹具解析**之前**判定：本用例所需的服务中，被 require 却不可达的服务。
    
        必须早于夹具解析：否则「同时需要缺失的 GITS 与被 require 且缺失的 KERT」的用例
        会先因 GITS 而 skip，把「被要求的服务没起来」伪装成合法跳过。
        只针对**本用例所需**的服务，因此不会牵连其它用例可独立验证的部分。
        """
        needed = _services_for(item)
        for key, service in SERVICES.items():
            if key in needed and _service_required(key) and not _ready(service):
>               raise ServiceNotReadyError(_required_message(service))
E               tests.e2e.conftest.ServiceNotReadyError: GITS Backend 不可达 (http://127.0.0.1:8082/actuator/health)，但已被显式要求可用（E2E_REQUIRE_GITS=1 或 E2E_REQUIRE_SERVICES=1）——禁止降级为 skip

tests/e2e/conftest.py:177: ServiceNotReadyError

E2E 覆盖账本（下限由本次收集结果推导，见 TECH_LEAD_DECISION.md D-8）：
  collected = 47
  passed  = 26   (下限 26 = 无服务 1 + 仅 KERT 25)
  skipped = 1   (上限 21 = 跨服务 21（仅 GITS 16 + 多端/含前端 5）)
  failed  = 0   (必须 0)
  errors  = 20   (必须 0)
  覆盖口径：26/47 真跑，1/47 未覆盖
  未覆盖用例清单（按文件分组）：
    tests/e2e/test_cross_service_health.py  (1 条)
      - TestCrossServiceHealth::test_gits_frontend_reachable  [Skipped: GITS Frontend 不可达 (http://127.0.0.1:5173)]
  require 开关：E2E_REQUIRE_GITS
  ✗ 下限断言失败：
    - errors=20（必须 0）
::error::E2E 覆盖账本下限断言失败，job 必须失败（禁止以 skip 掩盖未验证）
  说明：本账本是「未覆盖」的显式登记，不等于「通过」；CI 绿不代表跨服务链路已验证。
=========================== short test summary info ============================
ERROR tests/e2e/test_cross_service_health.py::TestCrossServiceHealth::test_gits_backend_health
ERROR tests/e2e/test_cross_service_health.py::TestCrossServiceHealth::test_all_services_simultaneously
ERROR tests/e2e/test_gits_to_kert_integration.py::TestGitsToKertIntegration::test_gits_calls_kert_skill_execute
ERROR tests/e2e/test_gits_to_kert_integration.py::TestGitsToKertIntegration::test_gits_kert_connectivity
ERROR tests/e2e/test_scenario_1_continuous_operation.py::TestContinuousOperation::test_start_journey
ERROR tests/e2e/test_scenario_1_continuous_operation.py::TestContinuousOperation::test_operating_view
ERROR tests/e2e/test_scenario_1_continuous_operation.py::TestContinuousOperation::test_gate_state_via_gits
ERROR tests/e2e/test_scenario_1_continuous_operation.py::TestContinuousOperation::test_full_engagement_loop
ERROR tests/e2e/test_scenario_2_previsit_report.py::TestPrevisitReport::test_memory_extraction
ERROR tests/e2e/test_scenario_2_previsit_report.py::TestPrevisitReport::test_full_previsit_flow
ERROR tests/e2e/test_scenario_3_service_proposal.py::TestServiceProposal::test_generate_proposal_via_gits
ERROR tests/e2e/test_scenario_3_service_proposal.py::TestServiceProposal::test_proposal_fact_labels
ERROR tests/e2e/test_scenario_3_service_proposal.py::TestServiceProposal::test_proposal_different_industry
ERROR tests/e2e/test_scenario_4_knowledge_graph.py::TestKnowledgeGraph::test_supply_chain_graph_via_gits
ERROR tests/e2e/test_scenario_4_knowledge_graph.py::TestKnowledgeGraph::test_supply_chain_node_types
ERROR tests/e2e/test_scenario_4_knowledge_graph.py::TestKnowledgeGraph::test_supply_chain_edges
ERROR tests/e2e/test_scenario_5_customer_insight.py::TestCustomerInsight::test_customer_operating_view
ERROR tests/e2e/test_scenario_5_customer_insight.py::TestCustomerInsight::test_kyc_gap_analysis
ERROR tests/e2e/test_scenario_5_customer_insight.py::TestCustomerInsight::test_insight_gate_correlation
ERROR tests/e2e/test_scenario_5_customer_insight.py::TestCustomerInsight::test_full_insight_flow
26 passed, 1 skipped, 20 errors in 35.61s
