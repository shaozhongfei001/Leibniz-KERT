# TL 裁定：合同 F 批次（c20-contract-merge）待决 7 项 + 2 条记录更正

```text
DECISION_ID : M7-CONTRACT-F-BATCH-RULINGS
DECIDED_BY  : Tech Lead
DECIDED_AT  : 2026-09-16
受文        : c20-contract-merge
性质        : TL 决定（不涉合同语义扩展）；凡属*扩展*的项一律转呈 Contract Owner
```

## 1. 裁定

| # | 事项 | 裁定 | 理由 |
|---|---|---|---|
| **①** | `additionalProperties: false` ×7 保留 or 回退 | **保留** | 它不是"与 v2 对齐"，而是**补上一个检出方向**——"合同声明了实现不存在的字段"（正是 F5 的缺陷形态）；且有实证：在 `GateAuditResponse` 塞幻影 `auditPath` ⇒ **有该关键字则 FAIL**、**去掉则漏检（7 passed）**。风险低：它约束**服务端响应形状**，不改运行时行为（FastAPI 不按此文档校验响应）。**范围限定**：只在**已加**的 7 处保留；**不**借机给全部 40 个 schema 铺开（那是另一决定，须逐条论证该响应确为闭集） |
| **②** | 4 个 Job 示例逐字段保留 or 精简（约 −60 行） | **保留逐字段** | ① v1 是**运行中权威**且被**另一仓（GITS）**消费，示例是其最便宜的文档；② schema 改了而示例不改 = 合同**内部矛盾**（F 类缺陷的形态）；③ 60 行 YAML 的成本远低于一次跨仓误读。日后要精简属**文档风格**决定，单独走 |
| **③** | F7 `/api/skill/health` 缺 `service` 声明 | **纳入本批** | 同属"按实现如实化"，非扩展 |
| **④** | F8 `/v1/*` **9 条**已实现未登记路径 | **不纳入本批**；须出**单独提案** | 声明它们属 **additive 契约扩展**（非形状更正）⇒ 必须走 **Contract Owner 追认**流程（与 ④ v1.5 同路径）。请只出提案文档（**不改合同**），由我转呈 |
| **⑤** | 候选方案 §1.2 的 `30 → 36` 更正 | **已授权**（前封） | 属更正**你自己交付物**的算术错误 |
| **⑥** | F3 "须通知 GITS" 是否走正式渠道 | **已授权**：更新本仓 `docs/integration/KERT_GITS_CONTRACT_DIFF.md` | 我核实该文件**在本仓**（8012 字节）；**不得**动 GITS 仓；是否送达由 Owner 定 |
| **⑦** | 版本 `1.5.0 → 1.5.1` | **已要求且已执行**（已见 `specs/kert-openapi-v1.yaml:40` = `1.5.1`，并已同步 `test_routing_api.py` 断言） | 追认对象唯一性（同 PREVISIT 地图版本口径） |

## 2. 记录更正（不改历史提交）

1. **合同 artifact sha 更正**：提交 `f7749af` 内的 `specs/kert-openapi-v1.yaml` 之 sha256 为
   **`bfe9cda57ff614f8300e1fd5ea49230707fa23f69fc93dbdff7dd6f780f8a26f`**。
   我此前（及 `f7749af` 提交说明中）记的 `40d4400a…` 是该文件在 c20 **恢复 `running` 示例（+27 行）之前**
   的中间态 ⇒ **我的记录有误**。历史提交不改写，以本条为准。
2. **c20 的一处自查值得记名**：其第一版**无意删除了 `running` 示例**（示例 4→3），
   审阅自身 diff 时**自查发现并恢复**。这类"自己抓自己"的行为按标杆记录。

## 3. 流程教训（新增规则，适用于所有后续片）

> **先冻结（提交）再跑全量 / 再 build。**
> 事实依据：我执行的一次全量套件（168s）恰逢 c20 在改 `specs/kert-openapi-v1.yaml`，
> 结果为 `1363 passed, 1 failed`（失败的是我那条版本断言 `== "1.5.0"`）；
> 隔离复跑**通过**，随后确认版本已按我的要求升到 `1.5.1` ⇒ **该次全量结果不可归因，不得作为证据**。
> 规则：**对"合同/schema/门面"的验证必须在冻结态（HEAD）上进行**；并发编辑期间的运行一律**不采信**。
> （与已有容器规则"先提交再 build"同源。）

## 4. 非声明

本文件是 TL 决定记录，**不是** Contract Owner 批准、**不是**合同修订；
F8 的 additive 扩展**未经追认**（仅授权出提案）；未改合同、未改 `03_core`、GITS 仓；`PRODUCTION_RELEASE_GATE=BLOCKED` 不变。
